Andreas Kromke
2018-05-10


MagiC kernel sources (DOS), converted to UTF-8.
Note that all the source files must be compiled separately, resulting in separate .o files.

Dependencies:

	MAGIDOS.S

     INCLUDE "..\ERRNO.INC"
     INCLUDE "KERNEL.INC"
     INCLUDE "STRUCTS.INC"
     INCLUDE "DEBUG.INC"

